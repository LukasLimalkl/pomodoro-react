import * as Styled from './styles';

export const Config = () => {
    return (
        <Styled.Container>
            <button>Start</button>
            <button>Pause</button>
            <button>Reniciar</button>
        </Styled.Container>
    );
};
