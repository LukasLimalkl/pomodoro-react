import * as Styled from './styles';

export const Interval = () => {
    return (
        <Styled.Container>
            <span>intervalo</span>
            <button>Start</button>
            <button>Reset</button>
        </Styled.Container>
    );
};
